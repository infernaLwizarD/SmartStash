<%
  @breadcrumbs = [
    {
      title: 'Коллекции',
      path: collection_items_path
    },
    {
      title: 'Добавить коллекцию'
    },
  ]
%>

<%=
  render 'form',
         url: collection_items_path(parent_id: params[:parent_id]),
         back_path: params[:parent_id] ? collection_item_path(params[:parent_id]) : collection_items_path
%>